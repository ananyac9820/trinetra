# Core runtime. This set alone is enough to build the cube, train, evaluate,
# serve every API route and render tiles. Nothing below it is required.
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
Pillow>=10.2
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9
httpx>=0.27
websockets>=12.0
pyarrow>=15.0

# Modelling. Torch is CPU-only here; the demo does not need a GPU.
torch>=2.2
timm>=0.9
xgboost>=2.0
properscoring>=0.1
